import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mandaliagarciel.nightlyshark',
  appName: 'NightlyShark',
  webDir: 'dist/nightly/browser'
};

export default config;
